# --- FastAPI & Server ---
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5

# --- Database ---
sqlalchemy==2.0.46
alembic==1.18.1

# --- ML / HuggingFace ---
# torch / torchaudio / torchvision werden vom Installationsskript als
# ROCm-Wheels installiert und sind hier NICHT enthalten.
#
# transformers 5.0.0 erfordert huggingface_hub>=1.3.0 (breaking change vs v4).
# accelerate>=1.1.0 ist laut transformers setup.py erforderlich.
transformers==5.0.0
accelerate==1.12.0
huggingface_hub==1.3.7
qwen-tts>=0.0.5

numba==0.60.0
llvmlite==0.43.0
librosa==0.11.0
# --- Audio ---
librosa==0.11.0
soundfile==0.13.1

# --- Audio Processing ---
imageio-ffmpeg==0.6.0
pydub==0.25.1

# --- Numerics ---
numpy==2.0.2

# --- Utilities ---
python-multipart==0.0.22